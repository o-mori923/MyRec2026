[project]
name = "myrec2026"
version = "2026.1.0"
description = "作業記録アプリ MyRec 2026"
requires-python = ">=3.11"
dependencies = [
    "pyinstaller>=6.20.0",
]

[dependency-groups]
dev = [
    "pillow>=12.2.0",
]
